using UnityEngine;

public class helloDev : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hello from the Dev!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
